package com;

public class Notes {
	
	/*
	 * 	Encapsulation
	 * -----------------
	 * 
	 * 	-> Binding of methods and properties together
	 * 
	 *  Rules
	 *  ---------
	 *  1. Every property/variables/fields in the class should be defined as private
	 *  2. Every method in the class should be defined as public with getXXX and setXXX
	 *  setXXX
	 *  -------
	 *  -> very setter will have arguments. But will not have return type.
	 *     return type always void
	 *  
	 *  getXXX
	 *  ------
	 *  -> Every getter will have return type.But will not have argument
	 *  
	 *  POJO (Plain old java object) / Model / Bean / Entity
	 *  --------------------------------------------------------
	 *  
	 *  
	 *  
	 *  
	 * 
	 */

}
