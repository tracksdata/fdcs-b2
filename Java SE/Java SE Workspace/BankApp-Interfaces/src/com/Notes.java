package com;

public class Notes {
	
	/*
	 * 
	 * 	1. A class can extends to another class
	 * 	2. A class Can not extends to a interface
	 * 	3. A class Can not extends to more than a class and interfaces
	 * 	4. A class can implements interface
	 * 	5. A class can not implements another class
	 * 	6. A class can not extends to an interface
	 * 
	 *  7. Interface can extends to another interface
	 *  8. Interface can extends more than one interface
	 *  9. Interface can not implements another interface
	 *  
	 *  10. A class can not be defined final and abstract at same time
	 *  
	 *  11. A class can extends to a class and can implements an interface at same time
	 *  
	 *  
	 *  
	 * 
	 * 
	 * 
	 */

}
