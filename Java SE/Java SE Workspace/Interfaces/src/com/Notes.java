package com;

public interface Notes{
	
	/*
	 *   Interfaces
	 *   -------------
	 *   	-> Interface is a method specification which contains zero or more
	 *   	   only abstract methods
	 *   	-> By default every method defined in the interface is public and abstract
	 *   	-> By default very variable in the interface is public static and final
	 *   	-> implements keyword should be used to implement any intreface
	 *   
	 *   
	 *    Notes
	 *    -------
	 */
	

}
